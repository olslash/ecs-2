{
  "name": "ecs-2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "jstransform --strip-types --harmony --watch src/ build/",
    "start": "node build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^3.9.3"
  },
  "devDependencies": {
    "jstransform": "^11.0.1"
  }
}
